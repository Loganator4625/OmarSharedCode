#include <iostream>
#include <string>
using namespace std;

char Encryptor(char usedLetter) {

	char encryptedLetter;

	encryptedLetter = usedLetter + 1;

	return encryptedLetter;
}

int main() {

	string usedLetter;

	cout << "Provide a letter" << endl;

	getline(cin, usedLetter);

	char inputLetter = usedLetter[0];

	cout << Encryptor(inputLetter) << endl;

	return 0;
}
